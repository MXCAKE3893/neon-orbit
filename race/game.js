const c=document.querySelector('#game'),x=c.getContext('2d'),S=document.querySelector('#score'),B=document.querySelector('#best'),G=document.querySelector('#boost'),O=document.querySelector('#overlay'),T=document.querySelector('#status'),H=document.querySelector('#help');let best=+localStorage.nightRunBest||0,score=0,boost=100,lane=1,cars=[],lines=0,last=0,spawn=0,mode='ready',keys={};B.textContent=String(best).padStart(4,'0');function start(){score=0;boost=100;lane=1;cars=[];spawn=0;mode='play';O.classList.add('hide');last=performance.now();requestAnimationFrame(loop)}function input(e){if(mode!=='play')return start();if((e.code==='ArrowLeft'||e.code==='KeyA')&&lane>0)lane--;if((e.code==='ArrowRight'||e.code==='KeyD')&&lane<2)lane++}onkeydown=e=>{keys[e.code]=1;if(!e.repeat)input(e)};onkeyup=e=>keys[e.code]=0;c.onpointerdown=e=>{if(mode!=='play')return start();let r=c.getBoundingClientRect();lane=(e.clientX-r.left)<r.width/2?Math.max(0,lane-1):Math.min(2,lane+1)};function crash(){mode='dead';best=Math.max(best,Math.floor(score));localStorage.nightRunBest=best;B.textContent=String(best).padStart(4,'0');O.classList.remove('hide');T.textContent='WRECKED';H.textContent=`DIST ${Math.floor(score)} / クリックして再走`}function loop(t){if(mode!=='play')return;let dt=Math.min((t-last)/1000,.04);last=t;let nitro=keys.Space&&boost>0;if(nitro)boost-=35*dt;else boost=Math.min(100,boost+10*dt);let speed=260+score*.25+(nitro?190:0);score+=dt*speed/20;lines=(lines+speed*dt)%120;spawn+=dt;if(spawn>Math.max(.38,.85-score/900)){cars.push({lane:Math.floor(Math.random()*3),y:-100,color:['#54f7ff','#ffc857','#ff5d61'][Math.floor(Math.random()*3)]});spawn=0}cars.forEach(a=>a.y+=speed*dt);cars=cars.filter(a=>a.y<700);if(cars.some(a=>a.lane===lane&&a.y>430&&a.y<555))return crash();S.textContent=String(Math.floor(score)).padStart(4,'0');G.textContent=`${Math.floor(boost)}%`;draw(nitro);requestAnimationFrame(loop)}function car(px,py,color,player=false){x.save();x.translate(px,py);x.fillStyle=color;x.shadowBlur=player?22:8;x.shadowColor=color;x.fillRect(-28,-50,56,100);x.fillStyle='#061018';x.fillRect(-19,-31,38,28);x.fillRect(-19,17,38,22);x.fillStyle=player?'#fff':'#ffefaa';x.fillRect(-22,-46,13,5);x.fillRect(9,-46,13,5);x.restore()}function draw(nitro=false){x.fillStyle='#060a0f';x.fillRect(0,0,900,600);x.fillStyle='#15191f';x.beginPath();x.moveTo(220,0);x.lineTo(680,0);x.lineTo(810,600);x.lineTo(90,600);x.fill();x.strokeStyle='#ff5d61';x.lineWidth=5;x.beginPath();x.moveTo(220,0);x.lineTo(90,600);x.moveTo(680,0);x.lineTo(810,600);x.stroke();x.strokeStyle='#fff5';x.lineWidth=4;x.setLineDash([55,65]);x.lineDashOffset=lines;for(let i=1;i<3;i++){x.beginPath();x.moveTo(220+i*153,0);x.lineTo(90+i*240,600);x.stroke()}x.setLineDash([]);for(let i=0;i<20;i++){x.fillStyle=i%2?'#ff5d6133':'#54f7ff22';x.fillRect((i*149)%900,(i*91+lines)%600,2,30)}cars.forEach(a=>car([250,450,650][a.lane],a.y,a.color));if(nitro){x.fillStyle='#54f7ff';x.beginPath();x.moveTo([250,450,650][lane]-14,560);x.lineTo([250,450,650][lane],600);x.lineTo([250,450,650][lane]+14,560);x.fill()}car([250,450,650][lane],500,'#f5f8ff',true)}draw();